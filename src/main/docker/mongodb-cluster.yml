version: '2'
services:
  actioncatalogservicesql-mongodb:
    image: mongo:4.2.7
    ports:
      - '27017:27017'
    command: mongos --configdb csvr/actioncatalogservicesql-mongodb-config --bind_ip 0.0.0.0
  actioncatalogservicesql-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --shardsvr --replSet rs1
  actioncatalogservicesql-mongodb-config:
    image: mongo:4.2.7
    container_name: actioncatalogservicesql-mongodb-config
    command: mongod --configsvr --dbpath /data/db --replSet csvr
